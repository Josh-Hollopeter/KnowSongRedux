import { SingleplayerQuestion } from './singleplayer-question.model';

describe('SingleplayerQuestion', () => {
  it('should create an instance', () => {
    expect(new SingleplayerQuestion()).toBeTruthy();
  });
});
